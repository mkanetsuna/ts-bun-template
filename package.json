{
    "name": "typescript-bun-template",
    "version": "1.0.0",
    "scripts": {
      "start": "bun run src/index.ts",
      "build": "bun build src/index.ts --outdir=dist",
      "dev": "bun --watch run src/index.ts"
    },
    "dependencies": {
    },
    "devDependencies": {
      "bun-types": "latest"
    }
  }